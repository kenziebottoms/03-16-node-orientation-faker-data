{
  "name": "faker-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "data:generate": "node ./build-data",
    "db:generate": "node ./build-db"
  },
  "dependencies": {
    "sqlite3": "^4.0.0"
  },
  "devDependencies": {
    "faker": "^4.1.0"
  }
}
